---
import ProjectLayout from '../../layouts/ProjectLayout.astro';

const title = 'MuMu 安卓环境网络调试与协议分析 — 项目详解';
const description = '基于 MuMu 模拟器、mitmproxy、Frida 17 与 ADB 构建的 Android 授权测试环境，记录测试 APK 的网络请求、SDK 连接、TLS 读写观测与调试报告沉淀。';
const slug = 'android-network-analysis';
const accent = 'oklch(70% 0.13 205)';
---

<ProjectLayout title={title} description={description} slug={slug} accent={accent}>
  <header class="hero">
    <div class="hero-grid">
      <div>
        <p class="kicker">MuMu Android Network Lab</p>
        <h1 transition:name="pt-android-network-analysis">MuMu 安卓环境网络调试与协议分析</h1>
        <p class="lead">以 MuMu 模拟器作为 Android 运行环境，配合 ADB、mitmproxy、Frida 17 和 Python 脚本，对自建测试 APK / 授权实验包的网络请求、SDK 连接和 Native 层读写行为进行记录与复盘。</p>
        <div class="tags"><span>MuMu 模拟器</span><span>ADB</span><span>mitmproxy</span><span>Frida 17</span><span>Python</span></div>
      </div>
      <div class="hero-visual" aria-hidden="true">
        <div class="visual-window">
          <div class="visual-dots"><span></span><span></span><span></span></div>
          <div class="packet"></div>
          <div class="packet"></div>
          <div class="packet"></div>
          <div class="phone"></div>
        </div>
      </div>
    </div>
  </header>

  <div class="stats">
    <div class="stat"><strong>30+</strong><span>请求与域名类型梳理</span></div>
    <div class="stat"><strong>Frida</strong><span>Native 层观测脚本</span></div>
    <div class="stat"><strong>TLS</strong><span>读写链路可视化</span></div>
    <div class="stat"><strong>MuMu</strong><span>模拟器调试环境</span></div>
  </div>

  <section>
    <h2>项目定位</h2>
    <p>这个项目不是简单写“会抓包”，而是把一次 Android 网络调试过程整理成可复用的实验环境：用 MuMu 模拟器运行测试 APK，通过 ADB 管理设备状态，用 mitmproxy 观察应用请求，再用 Frida 辅助记录 Native 层调用现象，最后沉淀为接口资产表和调试报告。</p>
    <p>测试对象采用自建 Android Demo APK 与授权课程实验包，覆盖登录态请求、列表接口、图片资源加载、埋点上报和第三方 SDK 连接等常见场景。这样既能展示真实移动端调试流程，也避免在公开作品集里暴露第三方应用的敏感信息。</p>
    <p class="note">公开页面只展示环境搭建思路、观察维度和报告产出，不公开认证材料、具体业务接口参数、可复现绕过脚本或第三方 App 的敏感数据。</p>
  </section>

  <section>
    <h2>测试环境</h2>
    <div class="grid">
      <div class="card"><h3>运行软件</h3><p>使用 MuMu 模拟器作为 Android 设备，创建干净测试实例，安装自建测试 APK，并通过 ADB 连接到本机调试环境。</p></div>
      <div class="card"><h3>观测工具</h3><p>桌面端使用 mitmproxy 记录 HTTP/TLS 请求，配合 Python 脚本整理域名、路径、状态码和资源类型。</p></div>
      <div class="card"><h3>辅助分析</h3><p>在授权测试包内使用 Frida 17 观察 connect、SSL_read、SSL_write 等调用现象，帮助判断请求链路和 Native 层通信位置。</p></div>
    </div>
  </section>

  <section>
    <h2>技术流程</h2>
    <div class="flow">
      <span>MuMu 实例与 ADB 准备</span>
      <span>mitmproxy 流量采集</span>
      <span>Frida Native 层观测</span>
      <span>接口资产与报告沉淀</span>
    </div>
  </section>

  <section>
    <h2>MuMu 使用过程</h2>
    <div class="steps">
      <div class="step"><div><h3>准备独立模拟器实例</h3><p>在 MuMu 中创建专门的测试环境，保持系统、代理、证书和测试 APK 配置独立，避免影响日常使用环境。</p></div></div>
      <div class="step"><div><h3>连接 ADB 与安装测试包</h3><p>通过 ADB 确认模拟器在线，安装自建 Demo APK，记录包名、版本号、运行入口和测试账号类型。</p></div></div>
      <div class="step"><div><h3>配置代理与采集请求</h3><p>将模拟器网络指向本机代理，在授权测试场景下打开登录、列表、图片加载、埋点等页面，采集请求链路和响应特征。</p></div></div>
      <div class="step"><div><h3>整理报告与复盘结论</h3><p>把域名、接口类型、SDK 连接、异常请求和 Native 层观察结果归档，输出调试报告、接口资产表和环境复现说明。</p></div></div>
    </div>
  </section>

  <section>
    <h2>核心实现</h2>
    <div class="grid">
      <div class="card"><h3>流量采集</h3><p>通过代理与日志链路观察测试 APK 的 HTTP/TLS 请求、SNI、静态资源、接口路径和第三方 SDK 连接，整理为接口资产清单。</p></div>
      <div class="card"><h3>动态观测</h3><p>使用 Frida 17 适配新版 API，对授权测试包中的 connect、SSL_read、SSL_write 等 Native 调用进行可视化记录。</p></div>
      <div class="card"><h3>报告输出</h3><p>将 MuMu 环境配置、测试路径、端点分类、链路判断和风险边界整理成 Markdown 报告，便于后续复盘和展示。</p></div>
    </div>
  </section>

  <section>
    <h2>作品集表达</h2>
    <p>这个项目适合放在“核心工程项目”里，重点体现移动端调试、网络协议理解、模拟器环境管理、动态插桩、问题拆解和文档化能力。相比只写“抓包”，更真实的表达是“用 MuMu 模拟器搭建 Android 授权测试环境，并把一次网络调试过程沉淀成可复盘的方法和报告”。</p>
  </section>
</ProjectLayout>

<style>
/* ═══ Hero 覆盖：双列布局（非居中）═══ */
.hero {
  padding: 3.6rem 0 3rem;
  text-align: left;
  background: none;
}
.hero h1 {
  font-size: clamp(2rem, 5vw, 3.2rem);
  line-height: 1.16;
  margin-bottom: 1rem;
  font-weight: 900;
}
.hero-grid {
  display: grid; grid-template-columns: minmax(0, 1fr) 340px;
  gap: 2rem; align-items: center;
}
.kicker {
  font-family: serif; color: var(--gold);
  font-size: 0.76rem; letter-spacing: 0.18em;
  text-transform: uppercase; margin-bottom: 0.8rem;
}
.lead { color: var(--text-secondary); font-size: 1rem; max-width: 650px; }
.tags { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: 1rem; }
.tags span {
  padding: 0.35rem 0.8rem; border: 1px solid var(--border);
  border-radius: 999px; color: var(--text-secondary);
  font-size: 0.82rem; background: oklch(100% 0 0 / 0.04);
}
.hero-visual {
  min-height: 260px; border: 1px solid var(--border);
  border-radius: 18px; padding: 1rem;
  background: linear-gradient(145deg, oklch(100% 0 0 / 0.08), transparent), oklch(7% 0.018 270 / 0.86);
  box-shadow: 0 24px 72px oklch(0% 0 0 / 0.32), inset 0 1px 0 oklch(100% 0 0 / 0.08);
}
.visual-window {
  height: 100%; min-height: 226px;
  border: 1px solid oklch(100% 0 0 / 0.1); border-radius: 14px;
  padding: 1rem; position: relative; overflow: hidden;
  background: oklch(6% 0.014 270 / 0.86);
}
.visual-dots { display: flex; gap: 6px; margin-bottom: 1rem; }
.visual-dots span { width: 8px; height: 8px; border-radius: 50%; background: var(--accent); }
.visual-dots span:nth-child(2) { background: oklch(76% 0.14 155); }
.visual-dots span:nth-child(3) { background: var(--gold); }
.packet {
  height: 28px; border-radius: 999px; margin: 0.55rem 0;
  background: linear-gradient(90deg, var(--accent), oklch(100% 0 0 / 0.08));
  opacity: 0.82;
}
.packet:nth-child(4) { width: 78%; opacity: 0.56; }
.packet:nth-child(5) { width: 58%; opacity: 0.42; }
.phone {
  position: absolute; right: 1rem; bottom: 1rem;
  width: 82px; height: 146px;
  border: 2px solid oklch(100% 0 0 / 0.18); border-radius: 24px;
  background: oklch(100% 0 0 / 0.055);
  box-shadow: inset 0 0 0 5px oklch(0% 0 0 / 0.24);
}
.phone::before {
  content: ''; position: absolute; inset: 18px 11px 24px;
  border-radius: 14px;
  background: linear-gradient(180deg, var(--accent), transparent 46%), oklch(100% 0 0 / 0.06);
}

/* ═══ Stats（不同于通用 .stats-strip）═══ */
.stats {
  display: grid; grid-template-columns: repeat(4, 1fr);
  gap: 1rem; margin: 2.2rem 0;
}
.stat {
  padding: 1.2rem; border: 1px solid var(--border);
  border-radius: 14px; background: var(--bg-raised);
  text-align: center;
}
.stat strong { display: block; color: var(--accent); font-size: 1.55rem; line-height: 1.15; }
.stat span { display: block; color: var(--text-muted); font-size: 0.78rem; margin-top: 0.3rem; }

/* ═══ Section ═══ */
section { padding: 2.6rem 0; border-bottom: 1px solid var(--border); }

/* h2 覆盖：无编号、适配 section 间距 */
h2 { margin: 0 0 1rem 0; display: block; }

/* ═══ Grid 和 Card（结构不同于通用 .card-grid）═══ */
.grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; margin-top: 1.1rem; }
.card h3 { font-size: 1rem; margin: 0 0 0.45rem; }
.card p { font-size: 0.9rem; line-height: 1.75; margin: 0; }

/* ═══ Flow ═══ */
.flow { display: grid; grid-template-columns: repeat(4, 1fr); gap: 0.8rem; margin-top: 1.1rem; }
.flow span {
  min-height: 76px; display: flex; align-items: center; justify-content: center;
  text-align: center; padding: 0.8rem; border: 1px solid var(--border);
  border-radius: 14px;
  background: linear-gradient(145deg, oklch(100% 0 0 / 0.06), transparent);
}

/* ═══ Steps（counter-reset 自动编号）═══ */
.steps { counter-reset: step; display: grid; gap: 0.8rem; margin-top: 1.1rem; }
.step {
  counter-increment: step; display: grid;
  grid-template-columns: auto 1fr; gap: 0.85rem; align-items: start;
  padding: 1rem; border: 1px solid var(--border); border-radius: 14px;
  background: oklch(100% 0 0 / 0.04);
}
.step::before {
  content: counter(step, decimal-leading-zero);
  display: grid; place-items: center;
  width: 38px; height: 38px; border-radius: 12px;
  background: oklch(70% 0.13 205 / 0.14); color: var(--accent);
  font-weight: 800;
}
.step h3 { font-size: 0.98rem; margin: 0 0 0.25rem; }
.step p { font-size: 0.9rem; line-height: 1.75; margin: 0; }

/* ═══ Responsive ═══ */
@media (max-width: 820px) {
  .hero-grid { grid-template-columns: 1fr; }
  .stats { grid-template-columns: repeat(2, 1fr); }
  .grid, .flow { grid-template-columns: 1fr; }
  .hero-visual { min-height: 230px; }
}
</style>
